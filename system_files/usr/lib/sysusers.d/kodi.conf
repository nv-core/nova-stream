# The kodi user owns the media session. Its home lives under /var because a
# bootc image ships no /var content — systemd materialises both on first boot.
u kodi - "Kodi Media Center" /var/lib/kodi /usr/sbin/nologin
