# Keep HDMI audio device numbering stable across reboots. Kodi stores the
# passthrough device by name, so a card that reorders itself is the usual cause
# of "audio worked yesterday" reports on multi-GPU or dock setups.
options snd slots=snd_hda_intel
options snd_hda_intel enable_msi=1
